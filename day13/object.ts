//Object
//objects are used to store multiple values in a single variable.
//syntax:
// let variableName: {key1: valueType1, key2: valueType2} = {key1: value1, key2: value2}    

/*let person: {name: string, age: number, isStudent: boolean} = {
    name: "Alice",
    age: 23,
    isStudent: true
};
console.log(person);

//using object type 
//object type can be defined using type alias or interface
//represent all values that are not primitive types (number, string, boolean, bigint, symbol, null, or undefined).

type Person = {
    name: string,
    age: number,
    isStudent: boolean
};
let student: Person = {
    name: "Bob",
    age: 21,
    isStudent: true
};
console.log(student);


//accessing object properties
//dot notation
console.log("Name:", person.name);
console.log("Age:", person.age);
console.log("Is Student:", person.isStudent);

//bracket notation
console.log("Name:", student["name"]);
console.log("Age:", student["age"]);
console.log("Is Student:", student["isStudent"]);

//modifying object properties
person.age = 24;
student.isStudent = false;
console.log("Updated Person:", person);
console.log("Updated Student:", student);

//inline object type
let employee: {id: number, name: string, department: string} = {
    id: 101,
    name: "Charlie",
    department: "HR"
};
console.log(employee);  

//aliasing object type
type Employee = {
    id: number,
    name: string,
    department: string
};
let manager: Employee = {
    id: 102,
    name: "David",
    department: "Finance"
};
console.log(manager);   


//using type alias
type Car = {
    make: string,
    model: string,  
    year: number
};
let myCar: Car = {  
    make: "Toyota",
    model: "Camry",
    year: 2020
};
console.log(myCar);*/

  